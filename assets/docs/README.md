# Docs

Drop the final deliverables here:

- `frames-that-speak-slides.pdf` — final presentation deck (export PowerPoint to PDF).
- `frames-that-speak-report.pdf` — final project report.

The deliverable buttons on the site link to these exact filenames. While the
files are missing, the buttons stay safe — JavaScript intercepts the click
and shows the "Coming soon" tooltip instead of returning a 404.
